name: 'Page Differences'
description: 'Take screenshots of static web page differences'
inputs:
  old-path:
    description: 'Path to old static pages.'
    required: true
  new-path:
    description: 'Path to new static pages.'
    required: true
  output-path:
    description: 'Path for saving final screenshots.'
    required: true
    default: page-differences-action/output
  temp-path:
    description: 'Path for saving temporary screenshots.'
    required: true
    default: page-differences-action/tmp
  port:
    description: 'Port for running local static web server'
    required: true
    default: 8000
outputs:
  path:
    description: 'Path to saved screenshots.'
runs:
  using: 'node12'
  main: 'index.js'